import React, { useState} from 'react';

export default function DashBoard() {

  return (
    <div className = 'dashboard'>
       <button className = 'get-started' >View Decks</button>
       <button className = 'get-started' >Create Deck</button>
    </div>
  )
}