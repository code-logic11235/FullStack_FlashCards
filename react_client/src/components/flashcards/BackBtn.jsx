import React, { useState} from 'react';




export default function BackBtn({}) {

  
  
return (
  <>
   <button className = 'get-started' 
   onClick={()=>{setShowCreateDeck(true)}}>Create New Subject</button>

  </>
)
}